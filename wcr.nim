let chores: = ["vacuuming", "cleaning"]
echo "how many times do you vacuum per week?"
var vacuuming = readLine(stdin);
echo "how many times do you clean per week?"
var cleaning = readLine(stdin);
echo "Depending on whether " + vacuuming + " or " + cleaning + " is higher, balance out the likelihood of doing it by daily." 
